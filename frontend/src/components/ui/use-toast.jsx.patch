--- frontend/src/components/ui/use-toast.jsx
+++ frontend/src/components/ui/use-toast.jsx
@@ -147,8 +147,13 @@
 }

 export function dispatchForTest(action) {
-  // vitest sets process.env.NODE_ENV
-  if (typeof window !== 'undefined' && window.__VITEST__) dispatch(action)
+  // @ts-ignore
+  if (
+    typeof process !== 'undefined' &&
+    // @ts-ignore
+    process.env &&
+    // @ts-ignore
+    process.env.NODE_ENV === 'test'
+  ) {
+    dispatch(action)
+  }
 }

 export { useToast, toast, Toaster }
